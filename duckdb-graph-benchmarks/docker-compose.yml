services:
  duckdb:
    image: duckdb/duckdb:latest
    container_name: duckdb
    volumes:
      - ./duckdb_data:/data
      - ./scripts:/scripts
    working_dir: /data
    entrypoint: ["duckdb", "/data/finbench.duckdb"]
    tty: true